<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title><%= user.username %>'s Pantry</title>
</head>
<body>
  <%- include('../partials/_navbar') %>
  
  <main>
    <h1><%= user.username %>'s Pantry</h1>
    <p>Explore what <strong><%= user.username %></strong> has in their pantry for inspiration!</p>
    
    <div class="pantry-navigation">
      <a href="/users">← Back to Community</a>
    </div>
    
    <% if (user.pantry.length === 0) { %>
      <p><%= user.username %> hasn't added any items to their pantry yet.</p>
    <% } else { %>
      <div class="pantry-items">
        <h2>Pantry Items (<%= user.pantry.length %>)</h2>
        <ul>
          <% user.pantry.forEach(function(food) { %>
            <li><%= food.name %></li>
          <% }); %>
        </ul>
      </div>
    <% } %>
  </main>
</body>
</html>
