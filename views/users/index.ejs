<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title>Explore Our Community</title>
</head>
<body>
  <%- include('../partials/_navbar') %>
  
  <main>
    <h1>Explore Our Community</h1>
    <p>Discover other users in our pantry community and find inspiration from their collections!</p>
    
    <% if (users.length === 0) { %>
      <p>No other users have joined the community yet. Be the first to share your pantry!</p>
    <% } else { %>
      <div class="users-list">
        <h2>Community Members</h2>
        <ul>
          <% users.forEach(function(communityUser) { %>
            <li>
              <a href="/users/<%= communityUser._id %>">
                <strong><%= communityUser.username %></strong>
              </a>
              <span> - <%= communityUser.pantry.length %> pantry items</span>
            </li>
          <% }); %>
        </ul>
      </div>
    <% } %>
  </main>
</body>
</html>
